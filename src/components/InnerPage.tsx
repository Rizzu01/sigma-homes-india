import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react';
export default function InnerPage({kicker,title,intro,sections}:{kicker:string;title:string;intro:string;sections:{title:string;body?:string;items?:string[]}[]}){return <main className="inner-page"><section className="inner-hero"><div className="container"><p className="kicker">{kicker}</p><h1>{title}</h1><p>{intro}</p></div></section><section className="inner-content"><div className="container">{sections.map((s,i)=><section className="content-block" key={s.title} data-aos="fade-up"><div><span className="content-no">{String(i+1).padStart(2,'0')}</span><h2>{s.title}</h2></div><div>{s.body&&<p>{s.body}</p>}{s.items&&<ul className="feature-list">{s.items.map(x=><li key={x}>{x}</li>)}</ul>}</div></section>)}<Link href="/contact" className="btn btn-primary">Talk to Sigma Homes <ArrowUpRight size={17}/></Link></div></section></main>}
